# Runtime dependencies for the server. cactus-needle itself is installed with
# --no-deps in the Dockerfile (see the comment there).
huggingface_hub>=0.23
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
